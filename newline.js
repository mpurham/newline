/*
    name: Newline
    description: Prints characters on a newline
    author: Marcell Purham
    tags: newline,space,characters
*/

function main(input) {
    input.text = input.text.replace(/ /g, '\n');
}
